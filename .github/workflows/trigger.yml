# name: Trigger Jenkins on PR Comment

# on:
#   issue_comment:
#     types: [created]

# jobs:
#   send-to-jenkins:
#     if: github.event.issue.pull_request != null && contains(github.event.comment.body, '@Dotnet-s390x')
#     runs-on: ubuntu-latest
#     steps:
#       - name: Send webhook to Jenkins
#         run: |
#           curl -X POST "http://148.100.85.217:8080/generic-webhook-trigger/invoke?token=mysecret123" \
#           -H "Content-Type: application/json" \
#           -d '{
#                 "pr_number": "'${{ github.event.issue.number }}'",
#                 "comment_body": "'${{ github.event.comment.body }}'",
#                 "comment_user": "'${{ github.event.comment.user.login }}'"
#               }'

name: Trigger Jenkins on Mention
on:
  issue_comment:
    types: [created]
permissions:
  issues: write
jobs:
  check-and-trigger:
    runs-on: ubuntu-latest
    if: |
      github.event.comment.body != '' &&
      contains(github.event.comment.body, '@Dotnet-s390x')

    steps:
      - name: Extract PR number from comment
        id: extract
        run: |
          echo "pr_number=$(jq --raw-output .issue.number "$GITHUB_EVENT_PATH")" >> "$GITHUB_OUTPUT"

      - name: Check Jenkins for duplicate PR build
        id: jenkins_check
        env:
          JENKINS_URL: http://148.100.85.217:8080
          JENKINS_USER: admin
          JENKINS_TOKEN: 11a2c7cba8bca546b72c395935572854db
          TARGET_JOB: build
          PR_NUMBER: ${{ steps.extract.outputs.pr_number }}
        run: |
            auth="-u $JENKINS_USER:$JENKINS_TOKEN"
            BUILD_INFO=$(curl -s $auth "$JENKINS_URL/job/$TARGET_JOB/lastBuild/api/json")
            
            is_running=$(echo "$BUILD_INFO" | jq -r '.building')
            
            if [ "$is_running" = "true" ]; then
              running_pr=$(echo "$BUILD_INFO" | jq -r '.actions[]?.parameters[]? | select(.name == "pr_number") | .value')
            else
              running_pr=""
            fi
            queued_prs=$(curl -s $auth "$JENKINS_URL/queue/api/json" | jq -r '
              .items[]? 
              | .actions[]?.parameters[]? 
              | select(.name == "pr_number") 
              | .value
            ')
            
            echo "running_pr=$running_pr"
            echo "queued_prs=$queued_prs"
            
            if [[ "$running_pr" == "$PR_NUMBER" ]] || echo "$queued_prs" | grep -q "^$PR_NUMBER$"; then
              echo "duplicate=true" >> "$GITHUB_OUTPUT"
            else
              echo "duplicate=false" >> "$GITHUB_OUTPUT"
            fi

      - name: Comment that build is already in progress
        if: steps.jenkins_check.outputs.duplicate == 'true'
        env:
          #GITHUB_TOKEN: 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
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          
        run: |
          PR_NUMBER=${{ steps.extract.outputs.pr_number }}
          COMMENT="A build is already running or queued for PR #${PR_NUMBER}. Ignoring this request."
          curl -s -H "Authorization: Bearer $GITHUB_TOKEN" \
               -H "Content-Type: application/json" \
               -X POST \
               -d "{\"body\": \"$COMMENT\"}" \
               "https://api.github.com/repos/${{ github.repository }}/issues/${PR_NUMBER}/comments"


      - name: Trigger Jenkins build
        if: steps.jenkins_check.outputs.duplicate == 'false'
        env:
          JENKINS_URL: http://148.100.85.217:8080
          JENKINS_USER: admin
          JENKINS_TOKEN: 11a2c7cba8bca546b72c395935572854db
        run: |
          curl -X POST "$JENKINS_URL/job/build/buildWithParameters" \
            --user "$JENKINS_USER:$JENKINS_TOKEN" \
            --data-urlencode "pr_number=${{ steps.extract.outputs.pr_number }}" \
            --data-urlencode "comment_user=${{ github.actor }}" \
            --data-urlencode "comment_body=${{ github.event.comment.body }}"
